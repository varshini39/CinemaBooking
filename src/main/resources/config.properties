# Default local-development configuration.
# Every key here can be overridden with an environment variable of the same
# name, uppercased with dots replaced by underscores (e.g. db.url -> DB_URL).
# Do not put real/production secrets in this file.

db.url=jdbc:mysql://localhost:3306/cinema
db.username=movie
db.password=test

redis.host=127.0.0.1
redis.port=6379

auth.username=ticketAdmin
auth.password=admin@123
